module.exports = {
    // Map of hooks
    hooks: {},

    // Map of new blocks
    blocks: {
      gitbook-plugin-annotate: {
        process: function(block){
          gitbook.events.bind("page.change", function() {
          console.log("Hello my plugin")
        }
      }
    },
    // Map of new filters
    filters: {}
};
